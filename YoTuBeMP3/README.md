# YouTube MP3 Downloader

Python-приложение для скачивания аудио с YouTube в формате MP3 с поддержкой битрейтов 128, 192, 320 kbps. Оптимизировано для использования в РФ с поддержкой прокси.

## 📋 Содержание

- [Возможности](#возможности)
- [Установка](#установка)
  - [Установка FFmpeg](#установка-ffmpeg)
  - [Установка Python зависимостей](#установка-python-зависимостей)
- [Быстрый старт](#быстрый-старт)
- [Использование](#использование)
  - [Базовое скачивание](#базовое-скачивание)
  - [Примеры с разными битрейтами](#примеры-с-разными-битрейтами)
  - [Использование прокси](#использование-прокси)
  - [Работа с плейлистами](#работа-с-плейлистами)
- [Аргументы CLI](#аргументы-cli)
- [Конфигурация](#конфигурация)
- [Прокси](#прокси)
- [Структура проекта](#структура-проекта)
- [Troubleshooting](#troubleshooting)
- [Лицензия](#лицензия)

## ✨ Возможности

- ⬇️ Скачивание аудио с YouTube в формате MP3
- 🎚️ Поддержка битрейтов: **128 kbps**, **192 kbps**, **320 kbps**
- 🌐 Полная поддержка прокси для обхода блокировок
- 🔄 Автоматическая ротация прокси при недоступности
- 📝 Добавление метаданных (теги, обложка, исполнитель)
- 🎵 Нормализация громкости
- 📋 Поддержка плейлистов
- 🎨 Цветной вывод в консоли
- 📊 Подробное логирование
- 🔒 Безопасное хранение настроек через .env

## 🚀 Установка

### Установка FFmpeg (ОБЯЗАТЕЛЬНО!)

**FFmpeg является обязательным компонентом** для конвертации аудио в MP3. Без него приложение работать не будет.

#### Windows

**Способ 1: Через Chocolatey (рекомендуется)**
```powershell
# Требуется установленный Chocolatey
choco install ffmpeg
```

**Способ 2: Ручная установка**
1. Перейдите на https://www.gyan.dev/ffmpeg/builds/
2. Скачайте версию `ffmpeg-release-essentials.zip`
3. Распакуйте в `C:\ffmpeg`
4. Добавьте `C:\ffmpeg\bin` в PATH:
   ```powershell
   [Environment]::SetEnvironmentVariable(
       "Path", $env:Path + ";C:\ffmpeg\bin", 
       "Machine"
   )
   ```
5. **Перезапустите терминал**

**Проверка установки:**
```powershell
ffmpeg -version
```

#### Linux (Debian/Ubuntu)

```bash
sudo apt update
sudo apt install ffmpeg
```

**Проверка:**
```bash
ffmpeg -version
```

#### macOS

```bash
brew install ffmpeg
```

**Проверка:**
```bash
ffmpeg -version
```

### Установка Python зависимостей

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd RSRS
```

2. Создайте виртуальное окружение (рекомендуется):
```bash
python -m venv venv
# Windows:
venv\Scripts\activate
# Linux/macOS:
source venv/bin/activate
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

## 🏃 Быстрый старт

```bash
# Скачать видео в MP3 (320 kbps по умолчанию)
python main.py "https://youtube.com/watch?v=VIDEO_ID"

# Скачать в 128 kbps (меньший размер файла)
python main.py "https://youtube.com/watch?v=VIDEO_ID" -b 128

# Скачать с прокси
python main.py "https://youtube.com/watch?v=VIDEO_ID" --proxy http://proxy:8080

# Показать справку
python main.py --help
```

## 📖 Использование

### Базовое скачивание

```bash
# Минимальный формат
python main.py "https://youtube.com/watch?v=dQw4w9WgXcQ"

# С указанием битрейта
python main.py "https://youtube.com/watch?v=dQw4w9WgXcQ" -b 320
```

### Примеры с разными битрейтами

#### 128 kbps
**Используйте для:** Подкастов, аудиокниг, когда важен размер файла
```bash
python main.py "https://youtube.com/watch?v=VIDEO_ID" -b 128
# Файл ~1 МБ на минуту аудио
```

#### 192 kbps
**Используйте для:** Качественного повседневного прослушивания
```bash
python main.py "https://youtube.com/watch?v=VIDEO_ID" -b 192
# Файл ~1.5 МБ на минуту аудио
```

#### 320 kbps
**Используйте для:** Максимального качества (студийное качество)
```bash
python main.py "https://youtube.com/watch?v=VIDEO_ID" -b 320
# Файл ~2.5 МБ на минуту аудио
```

### Использование прокси

#### Одиночный прокси

```bash
# HTTP прокси с авторизацией
python main.py "https://youtube.com/watch?v=VIDEO_ID" --proxy http://user:pass@proxy.example.com:8080

# HTTPS прокси
python main.py "https://youtube.com/watch?v=VIDEO_ID" --proxy https://proxy.example.com:443

# SOCKS5 прокси
python main.py "https://youtube.com/watch?v=VIDEO_ID" --proxy socks5://proxy.example.com:1080
```

#### Файл с прокси

Создайте файл `proxies.txt`:
```text
http://user:pass@proxy1.example.com:8080
http://user:pass@proxy2.example.com:8080
socks5://proxy3.example.com:1080
```

Запустите:
```bash
python main.py "https://youtube.com/watch?v=VIDEO_ID" --proxy-file proxies.txt
```

Приложение автоматически протестирует каждый прокси и использует рабочий.

#### Прокси для обхода блокировок РФ

```bash
# Пример с российским прокси
python main.py "https://youtube.com/watch?v=VIDEO_ID" \
  --proxy http://ru-proxy.example.com:8080 \
  -b 320

# Ротация прокси из файла
python main.py "https://youtube.com/watch?v=VIDEO_ID" \
  --proxy-file proxies_ru.txt \
  --proxy-rotate
```

### Работа с плейлистами

```bash
# Скачать весь плейлист
python main.py "https://youtube.com/playlist?list=PLAYLIST_ID"

# Первые 10 треков из плейлиста
python main.py "https://youtube.com/playlist?list=PLAYLIST_ID" --playlist-items 1-10

# С определенного номера до конца
python main.py "https://youtube.com/playlist?list=PLAYLIST_ID" --playlist-items 5-
```

## 🔧 Аргументы CLI

| Аргумент | Описание | Пример |
|----------|----------|--------|
| `url` | URL видео или плейлиста | `python main.py "https://youtube.com/watch?v=..."` |
| `-b, --bitrate` | Битрейт (128, 192, 320) | `-b 320` |
| `-o, --output` | Выходная директория | `-o ./music` |
| `--proxy` | Прокси сервер | `--proxy http://proxy:8080` |
| `--proxy-file` | Файл с прокси | `--proxy-file proxies.txt` |
| `--proxy-rotate` | Ротация прокси | `--proxy-rotate` |
| `--ffmpeg-path` | Путь к FFmpeg | `--ffmpeg-path /usr/bin/ffmpeg` |
| `-v, --verbose` | Подробный вывод | `-v` |
| `--list-formats` | Показать форматы | `--list-formats` |
| `--no-metadata` | Без метаданных | `--no-metadata` |
| `--normalize` | Нормализовать громкость | `--normalize` |
| `--playlist-items` | Номера треков | `--playlist-items 1-10` |
| `-h, --help` | Показать справку | `-h` |

### Примеры использования аргументов

```bash
# Максимальное качество + прокси + метаданные
python main.py "https://youtube.com/watch?v=VIDEO_ID" -b 320 --proxy http://proxy:8080

# Экономия места + без метаданных
python main.py "https://youtube.com/watch?v=VIDEO_ID" -b 128 --no-metadata

# Плейлист в свою папку
python main.py "https://youtube.com/playlist?list=ID" -o ./my_music -b 192

# Ручной путь к FFmpeg (если не в PATH)
python main.py "https://youtube.com/watch?v=VIDEO_ID" --ffmpeg-path "C:\ffmpeg\bin\ffmpeg.exe"

# Комбинация всех опций
python main.py "https://youtube.com/watch?v=VIDEO_ID" \
  -b 320 \
  -o ./downloads \
  --proxy http://user:pass@proxy:8080 \
  --normalize \
  -v
```

## ⚙️ Конфигурация

Создайте файл `.env` на основе `.env.example`:

```env
# Настройки скачивания
DEFAULT_BITRATE=320
OUTPUT_DIR=./output
FFMPEG_PATH=

# Прокси (опционально)
PROXY=http://user:pass@proxy:8080
PROXY_FILE=proxies.txt

# Дополнительные опции
NORMALIZE_VOLUME=true
ADD_METADATA=true
VERBOSE=false
```

## 🔒 Прокси

### Формат прокси

```text
# HTTP с авторизацией
http://user:password@host:port

# HTTP без авторизации
http://host:port

# HTTPS
https://host:port

# SOCKS5
socks5://host:port
```

### Где взять прокси

**Бесплатные (для тестирования):**
- https://free-proxy-list.net
- https://www.proxy-list.download
- https://geonode.com/free-proxy-list

**Премиум прокси (для надежной работы):**
- SmartProxy
- Bright Data
- Oxylabs

### Ротация прокси

При указании нескольких прокси в файле приложение:
1. Тестирует каждый прокси перед использованием
2. Автоматически переключается на следующий при недоступности
3. Ведет лог использованных прокси

## 📁 Структура проекта

```
RSRS/
├── src/
│   ├── __init__.py
│   ├── __main__.py          # Точка входа приложения
│   ├── config/
│   │   ├── __init__.py
│   │   ├── settings.py      # Конфигурация и настройки
│   │   └── proxies.py       # Менеджер прокси
│   ├── core/
│   │   ├── __init__.py
│   │   ├── downloader.py    # Загрузчик (yt-dlp)
│   │   ├── converter.py     # Конвертер в MP3 (FFmpeg)
│   │   └── metadata.py      # Метаданные и теги
│   ├── cli/
│   │   ├── __init__.py
│   │   ├── parser.py        # Argparse парсер
│   │   └── output.py        # Форматированный вывод
│   └── utils/
│       ├── __init__.py
│       ├── validators.py    # Валидация URL
│       ├── path_utils.py    # Работа с путями
│       └── logging_utils.py # Логирование
├── logs/                    # Логи приложения
├── output/                  # Скачанные файлы
├── .env.example             # Пример конфигурации
├── requirements.txt         # Python зависимости
├── main.py                  # Основной скрипт
└── README.md                # Документация
```

## 🐛 Troubleshooting

### Часто задаваемые вопросы

#### 1. FFmpeg не найден

**Ошибка:**
```
FileNotFoundError: [Errno 2] No such file or directory: 'ffmpeg'
```

**Решение:**
1. Проверьте установку: `ffmpeg -version`
2. Если не установлен - [установите FFmpeg](#установка-ffmpeg-обязательно)
3. Если установлен, но не в PATH:
   ```bash
   python main.py "https://youtube.com/watch?v=VIDEO_ID" --ffmpeg-path /полный/путь/к/ffmpeg
   ```

#### 2. Ошибка прокси

**Ошибка:**
```
ProxyError: Failed to establish connection
```

**Решение:**
1. Проверьте формат прокси: `http://user:pass@host:port`
2. Убедитесь, что прокси рабочий (проверьте в браузере)
3. Попробуйте другой прокси из списка
4. Если используете файл - проверьте кодировку (UTF-8)

#### 3. YouTube заблокирован

**Ошибка:**
```
ExtractorError: Sign in to confirm your age
```
или
```
HttpError 403: Forbidden
```

**Решение:**
1. Используйте прокси с российским IP
2. Попробуйте SOCKS5 прокси
3. Обновите yt-dlp: `pip install -U yt-dlp`

#### 4. Видео недоступно

**Ошибка:**
```
UnavailableError: Video unavailable
```

**Решение:**
1. Проверьте URL видео
2. Видео может быть удалено или приватным
3. Возможно, видео недоступно в вашем регионе - используйте прокси

#### 5. Медленное скачивание

**Симптом:** Скачивание идет очень медленно

**Решение:**
1. Используйте прокси
2. Уменьшите битрейт: `-b 128`
3. Проверьте скорость интернета
4. Попробуйте скачать в другое время

#### 6. Ошибка памяти на больших плейлистах

**Симптом:** MemoryError при скачивании длинных плейлистов

**Решение:**
1. Скачивайте плейлист частями: `--playlist-items 1-50`
2. Увеличьте размер файла подкачки
3. Закройте другие приложения

#### 7. Метаданные не добавляются

**Симптом:** MP3 файл без тегов и обложки

**Решение:**
1. Проверьте, что yt-dlp установлен: `pip install -U yt-dlp`
2. Убедитесь, что видео имеет метаданные
3. Попробуйте без флага `--no-metadata`

#### 8. Битый MP3 файл

**Симптом:** Скачанный файл не воспроизводится

**Решение:**
1. Удалите файл и скачайте заново
2. Проверьте, что FFmpeg установлен корректно
3. Попробуйте другой битрейт

### Проверка окружения

```bash
# Проверка Python версии
python --version

# Проверка FFmpeg
ffmpeg -version

# Проверка yt-dlp
yt-dlp --version

# Проверка установленных пакетов
pip list | grep -E "yt-dlp|requests|colorama"
```

### Логи

Подробные логи сохраняются в директории `logs/`:
- `app.log` - основные логи
- `error.log` - только ошибки

Для отладки используйте флаг `-v`:
```bash
python main.py "https://youtube.com/watch?v=VIDEO_ID" -v
```

## 📝 Лицензия

MIT License

## 🤝 Вклад

Pull requests приветствуются!

## 📧 Контакты

Для вопросов и предложений используйте Issues.
